import { SearchByBookNamePipe } from './search-by-book-name.pipe';

describe('SearchByBookNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByBookNamePipe();
    expect(pipe).toBeTruthy();
  });
});
